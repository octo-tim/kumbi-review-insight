[build]
builder = "nixpacks"

[deploy]
# startCommand는 Procfile에 정의되어 있으므로 여기서 덮어쓰지 않음
healthcheckPath = "/_stcore/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
